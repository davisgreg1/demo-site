import { siteConfig } from "@/config/site.config";

export const seoContent = {
  title: `${siteConfig.business.name} | Atlanta Plumber`,
  description: siteConfig.business.tagline,
  keywords: siteConfig.seo.primaryKeywords,
};
